// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id    Int     @id @default(autoincrement())
  name  String  
  email String @unique
}

// model Address {
//   id      Int     @id @default(autoincrement())
//   street  String
//   city    String  
//   zipCode String  @db.Text(10)
//   dsdasd  String  
//   // zipCode String  @db.Text(10) // Limite de 10 caracteres

// }
